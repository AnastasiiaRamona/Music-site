const nextConfig = {
  compress: true,
  poweredByHeader: false,
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    inlineCss: true,
    useLightningcss: true,
  },
};

export default nextConfig;
